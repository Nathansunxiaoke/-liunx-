基于Linux的多用户航班购票系统

操作步骤如下：
1、首先进入到project目录下

2、在分别区server/client/命令行执行make

3、在命令行执行ifconfig查看IP地址

4、在命令行执行./bin/server端口号（端口号可以是1024~65535之间的数）

5、在命令行执行./bin/client  IP地址 端口号（与服务器的端口号保持一致）

6、执行完以上命令即可
